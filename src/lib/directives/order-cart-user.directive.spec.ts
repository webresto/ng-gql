import { OrderCartUserDirective } from './order-cart-user.directive';

describe('OrderCartUserDirective', () => {
  it('should create an instance', () => {
    const directive = OrderCartUserDirective;
    expect(directive).toBeTruthy();
  });
});
