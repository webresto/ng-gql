import { DishCalcDirective } from './dish-calc.directive';

describe('DishCalcDirective', () => {
  it('should create an instance', () => {
    const directive = DishCalcDirective;
    expect(directive).toBeTruthy();
  });
});
