import { SetDishCommentDirective } from './set-dish-comment.directive';

describe('SetDishCommentDirective', () => {
  it('should create an instance', () => {
    const directive = SetDishCommentDirective;
    expect(directive).toBeTruthy();
  });
});
