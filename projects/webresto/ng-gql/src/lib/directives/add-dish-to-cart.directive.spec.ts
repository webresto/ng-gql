import { AddDishToCartDirective } from './add-dish-to-cart.directive';

describe('AddDishToCartDirective', () => {
  it('should create an instance', () => {
    const directive = AddDishToCartDirective;
    expect(directive).toBeTruthy();
  });
});
