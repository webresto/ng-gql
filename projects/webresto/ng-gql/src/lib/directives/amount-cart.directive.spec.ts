import { AmountCartDirective } from './amount-cart.directive';

describe('AmountCartDirective', () => {
  it('should create an instance', () => {
    const directive = AmountCartDirective;
    expect(directive).toBeTruthy();
  });
});
