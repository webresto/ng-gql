import { DeleteFromCartDirective } from './delete-from-cart.directive';

describe('DeleteFromCartDirective', () => {
  it('should create an instance', () => {
    const directive = DeleteFromCartDirective;
    expect(directive).toBeTruthy();
  });
});
