import { ModifiersDirective } from './modifiers.directive';

describe('ModifiersDirective', () => {
  it('should create an instance', () => {
    const directive = ModifiersDirective;
    expect(directive).toBeTruthy();
  });
});
