stages:
  - publish

publish:
  stage: publish
  only: [master] 
  script:
    - $NVM_DIR/nvm install 16
    - nvm use 16
    - cp ./.ci/.npmrc ./.npmrc 
    - npm i 
    - npm run build 
    - cd ./dist && npm publish && cd .. && rm -rf ./.npmrc
