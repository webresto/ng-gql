stages:
  - publish

publish:
  stage: publish
  only: [master] 
  script:
    - cp ./.ci/.npmrc ./.npmrc 
    - nvm use 14
    - npm i 
    - npm run build 
    - cd ./dist && npm publish && cd .. && rm -rf ./.npmrc
